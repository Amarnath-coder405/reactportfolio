{
  "name": "reactportfolio",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://Amarnath-coder405.github.io/",
  "dependencies": {
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "^5.0.1",
    "sticky-kit": "^1.1.3",
    "travis": "^0.1.1",
    "webpack": "^5.101.2",
    "yarn": "^1.13.0"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
    
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
